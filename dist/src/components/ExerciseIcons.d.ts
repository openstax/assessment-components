import { ExerciseData } from '../../src/types';
export declare const ExerciseIcons: ({ exercise, topicUrl, errataUrl }: {
    exercise: ExerciseData;
    topicUrl?: string | undefined;
    errataUrl?: string | undefined;
}) => JSX.Element;
